apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hr-analytics-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/PavloYaremchuk/open-data-ai-analytics.git'
    targetRevision: HEAD
    path: gitops/app
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: hr-analytics
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
