apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: hr-analytics
spec:
  type: LoadBalancer
  selector:
    app: web-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
